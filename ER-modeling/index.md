# Entity Relationship Modeling (ER-modeling)

- Entity word refers to a thing or a concept (abstract thing).
- Relationship word refers to a connection between two or more entities.
- Modeling word refers to a representation of entities and relationships.

## Questions

` Q.) In age of AI, Why i spend time to know about ER-modeling ? `
` Q.) Why is need of modeling relationship between entities ? `
` Q.) What is its need ? `

` Q.) Is there alternative solution ? `